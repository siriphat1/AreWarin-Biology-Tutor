# AreWarin public Edge Functions.
# Public student/tutor forms are not authenticated; validation and privileged DB writes
# are performed inside the functions using server-side environment credentials.

[functions.create-enrollment]
verify_jwt = false

[functions.get-receipt]
verify_jwt = false

[functions.submit-tutor-application]
verify_jwt = false
