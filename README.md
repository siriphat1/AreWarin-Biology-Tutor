# AreWarin Student Standalone V15.6.4

This build inlines the full Student theme inside `index.html`.
Upload only `/student/index.html`. No separate theme CSS is required.

Fixes login UI controls that appeared as native browser buttons when the external CSS file was missing or cached incorrectly.

No SQL or Edge Function changes.
